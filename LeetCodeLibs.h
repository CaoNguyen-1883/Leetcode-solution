#ifndef LEETCODELIBS_H
#define LEETCODELIBS_H

#include <iostream>      // Nhập/xuất dữ liệu
#include <vector>        // Mảng động (vector)
#include <array>         // Mảng tĩnh (array)
#include <string>        // Xử lý chuỗi
#include <sstream>       // Xử lý chuỗi dạng stream
#include <algorithm>     // Các thuật toán (sort, reverse, unique, ...)
#include <numeric>       // Các hàm toán học (accumulate, gcd, lcm, ...)
#include <cmath>         // Toán học (sqrt, pow, ceil, floor, ...)
#include <limits>        // Giới hạn kiểu dữ liệu
#include <map>          // Cấu trúc dữ liệu ánh xạ (map, unordered_map)
#include <unordered_map> // Bảng băm (unordered_map)
#include <set>          // Tập hợp (set, unordered_set)
#include <unordered_set> // Tập hợp không có thứ tự (unordered_set)
#include <queue>        // Hàng đợi (queue, priority_queue)
#include <stack>        // Ngăn xếp (stack)
#include <deque>        // Hàng đợi hai đầu (deque)
#include <list>         // Danh sách liên kết (list)
#include <bitset>       // Xử lý bit
#include <tuple>        // Tuple (bộ giá trị nhiều kiểu)
#include <functional>   // Hàm và toán tử
#include <cassert>      // Kiểm tra điều kiện (assert)
#include <chrono>       // Đo thời gian

using namespace std;

#endif