#include"LeetCodeLibs.h"

class Solution {
public:
    bool exist(vector<vector<char>>& board, string word) {
        int a[128];
        for(int& c : word){
            ++a[c];
        }

        for(auto& v : board){
            for(int& i : v){
                
            }
        }
    }
};